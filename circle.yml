machine:
  xcode:
    version: 8.3.2
  node:
    version: 7.10.0

#deployment:
#  beta_distribution:
#    branch: circleci
#    commands:
#      - gym --scheme "Tokopedia"
#      - ipa distribute:crashlytics
#          --crashlytics_path Crashlytics.framework
#          --api_token    "$CRASHLYTICS_API_KEY"
#          --build_secret "$CRASHLYTICS_SECRET"

