machine:
  environment:
    XCODE_SCHEME: "Tokopedia"

  xcode:
    version: 8.3.2

dependencies:
  override:
    - brew install node
    - npm install

# deployment:
#   production:
#     branch: master
#     commands:
#       - fastlane beta

