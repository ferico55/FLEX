machine:
  environment:
    XCODE_SCHEME: "Tokopedia"

  xcode:
    version: 8.3.2

dependencies:
  override:
    - brew install node
    - brew install yarn
    - npm install
    - yarn

test:
  override:
    - yarn danger

# deployment:
#   production:
#     branch: master
#     commands:
#       - fastlane beta

